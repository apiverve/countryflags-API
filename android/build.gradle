plugins {
    id 'java-library'
    id 'maven-publish'
}

group = 'com.apiverve'
version = '1.1.12'

java {
    sourceCompatibility = JavaVersion.VERSION_1_8
    targetCompatibility = JavaVersion.VERSION_1_8
}

dependencies {
    implementation 'org.json:json:20231013'
}

publishing {
    publications {
        maven(MavenPublication) {
            groupId = 'com.apiverve'
            artifactId = 'countryflags-api'
            version = '1.1.12'

            from components.java

            pom {
                name = 'Country Flags'
                description = 'Country Flag is a simple tool for getting the country's national flags. It returns the country's national flags.'
                url = 'https://apiverve.com'

                licenses {
                    license {
                        name = 'MIT License'
                        url = 'https://opensource.org/licenses/MIT'
                    }
                }

                developers {
                    developer {
                        id = 'apiverve'
                        name = 'APIVerve'
                        email = 'hello@apiverve.com'
                    }
                }

                scm {
                    connection = 'scm:git:git://github.com/apiverve/countryflags-api.git'
                    developerConnection = 'scm:git:ssh://github.com/apiverve/countryflags-api.git'
                    url = 'https://github.com/apiverve/countryflags-api'
                }
            }
        }
    }
}
